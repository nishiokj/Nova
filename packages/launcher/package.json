{
  "name": "launcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --outdir dist --target node",
    "lint": "bun tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "@types/node": "^20.10.0"
  }
}
