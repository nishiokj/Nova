# Core dependencies for rex voice agent
# Audio dependencies are in requirements-audio.in
# Install with: pip install -r requirements.txt
# For audio support: pip install -r requirements-audio.txt
# Or use: pip install -e ".[audio]"

# LLM Backends
openai>=1.0.0
anthropic>=0.20.0

# Web Tools
requests>=2.28.0
beautifulsoup4>=4.12.0
html2text>=2020.1.16

# HTTP client (OpenAI's SDK adds proxies to httpx calls; old httpx versions reject that keyword).
httpx==0.27.2

# Async HTTP client (for Gemini image generation)
aiohttp>=3.9.0

# Evaluation System & Data Processing
# numpy>=2.3 requires Python>=3.11, so lock to the latest 3.10-compatible release.
numpy<=2.2.6
scipy<=1.16.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
typing_extensions==4.14.0

# Optional: For timezone support
pytz>=2023.3
